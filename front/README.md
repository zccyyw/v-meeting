# Front (@meeting/front)

浏览器端会议 UI：Vite + React + PWA + mediasoup-client。

```powershell
# 从仓库根目录
npm run dev:front
# 或
npm run build -w @meeting/front
```

依赖共享包 `@meeting/shared`（位于 `../serve/shared`）。