# 需求实现分析清单

> 基于需求文件 `docs/xuqiu/meet-需求-260819.txt`，对照最新代码逐条分析。
> 分析日期：2026-09-07（第二轮复核，含本轮修复内容）

---

## 需求总览

| 状态 | 数量 | 说明 |
|------|------|------|
| ✅ 已完成 | 15 | 代码已实现，功能可用 |
| ⚠️ 部分完成 | 0 | — |
| ❌ 未实现 | 0 | — |
| N/A | 2 | 硬件依赖 / 商务条款 |

> 2026-09-03 第一轮分析中的 P1/P2 缺口（邀请面板、监控页、密码策略默认值、密码加密传输、信令名单状态同步、首页优先级排序等）已在 09-03 ~ 09-07 期间全部补齐；本轮（09-07）完成剩余 4 项语义确认后的实现，见「本轮修复记录」。

---

## 逐条分析

### 需求 1：会议前小分屏显示被邀请人员入会情况 + 主讲人视频默认右下角可拖动

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 会议前小分屏显示被邀请人员（在线/离线） | ✅ | `InvitationPanel` 组件：姓名 + 单位 + 状态圆点（绿/灰）+ 状态文字（在会议中/已入会/待入会），5 秒轮询刷新；在会议连接中/等候室阶段由"名单"按钮浮出（`MeetingPage.tsx` connecting/waiting 分支）。后端 `GET /meetings/:id/invitations` 返回 `deptName`（JOIN sys_dept）；信令 join/leave/准入实时更新 `meeting_invitations.status` |
| 主讲人视频默认右下角、可拖动 | ✅ | `VideoGrid.tsx` 新增 `DraggablePip`：speaker/training 布局（含共享场景）下，主屏显示共享画面或焦点发言人，主讲人（主持人）视频以小窗悬浮主屏右下角，可自由拖动；无共享且无焦点切换时主讲人自动成为主屏画面（不重复显示 PIP） |

**涉及代码**：
- `front/src/components/InvitationPanel.tsx`、`front/src/components/VideoGrid.tsx`（DraggablePip）
- `front/src/pages/MeetingPage.tsx`（名单面板 + hostPeerId 传递）
- `serve/api/src/routes/meetings.ts`（invitations 返回 deptName）
- `serve/realtime/src/signal.ts`（join/leave/admit 更新名单状态）

---

### 需求 2：会议管理后端查看所有参会单位在线情况

| 状态 | 分析 |
|------|------|
| ✅ | 需求确认："参会单位 = 参会用户"。`MeetingMonitorPage`（10 秒轮询）展示所有会议的在线人数/邀请数，展开行显示每位参会用户（姓名/部门/状态/加入时间），支持部门筛选；菜单对 admin/auth_admin/audit_admin 可见。后端 `GET /admin/meetings/online`、`GET /admin/meetings/:id/attendees` |

---

### 需求 3：培训模式（主讲人小屏 + 右侧列表 + 点击切换 + 可收缩）

| 状态 | 分析 |
|------|------|
| ✅ | training 布局：主画面 + 右侧参会人员列表（`DraggableSideList`）；列表可整体拖动浮动；**可收缩/展开**（列表右上角按钮收起为窄条，点击恢复）；培训模式下点击列表项或主画面切换焦点 |

---

### 需求 4：一键群组（含常用分组）

| 状态 | 分析 |
|------|------|
| ✅ | `GroupManagePage`：创建/编辑/删除多个群组、一键快速开会（自动建会 + 邀请全部成员，含等候室选项）；**常用分组已实现**——`meeting_groups.pinned` 列 + `POST /meeting-groups/:id/pin` + 前端星标按钮，常用群组置顶显示并带"常用"金色标签。*2026-09-07 补齐* |

---

### 需求 5：发起者可以切换发言者单位画面

| 状态 | 分析 |
|------|------|
| ✅ | 需求确认：仅发起者/主持人可切换。`MeetingPage` 仅在 `isHost` 时传入 `onFocusPeer`，培训布局下主持人点击任意参会者即切换主屏画面；其他参会者不可切换 |

---

### 需求 6.1：三员账号

| 状态 | 分析 |
|------|------|
| ✅ | 系统管理员 sys_admin（role_id=3）：用户/部门管理 + 参数设置；授权管理员 auth_admin（role_id=4）：用户审批 + 会议审批 + 登录日志；审计管理员 audit_admin（role_id=5）：除自己外所有账号的操作日志（`sys-operlog.ts:66-67` 排除本人）。三员权限互斥 |

### 需求 6.2：页面停留 10 分钟自动退出

| 状态 | 分析 |
|------|------|
| ✅ | `useIdleTimer` 默认 600 秒（配置项 `sys.session.idleTimeout` 可调），超时前 60 秒警告弹窗倒计时，超时登出并释放在线名额。会议应用 `AppShell` 与**管理后台 `ManagerShell` 均已挂载**；会议页（`/m/:id`）独立于 Shell，不会在会议中误退出 |

### 需求 6.3：密码策略

| 状态 | 分析 |
|------|------|
| ✅ | minLength=10（`008_sys_security.sql:72`）、复杂度校验（字符类别组合）、过期天数 15（:93）、首次登录/默认密码强制改密提示 |

### 需求 6.4：密码加密传输

| 状态 | 分析 |
|------|------|
| ✅ | 前端 `encryptPassword`（AES-GCM，`shared/src/crypto.ts`）+ HTTPS 双层保护；覆盖登录、改密、管理员建户/重置密码、用户注册审批。*加固建议：升级为 RSA 非对称方案（见待完善清单）* |

### 需求 6.5：用户锁定/解锁

| 状态 | 分析 |
|------|------|
| ✅ | 连续失败 5 次锁定，锁定时长 60 秒自动解除，锁定事件写入登录日志 |

### 需求 6.6：日志

| 状态 | 分析 |
|------|------|
| ✅ | 字段含时间/IP/操作人/模块/类型/参数/结果；按字段筛选；导出；保留 365 天（≥12 个月）；存储阈值 80% 可配置 |

---

### 需求 7：省里会议投屏到市会议界面

| 状态 | 分析 |
|------|------|
| N/A | 需求文件已标注"暂不实现，需要硬件支持" |

---

### 需求 8：会议申请审批（预约会议纳入审批，快速会议不变）

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 会议申请（时间/时间段、地点、单位数量、名称、优先级） | ✅ | 首页"预定会议"弹窗提交申请：主题、预约时间、结束时间、地点、单位数量、优先级（高/中/低，默认中）；调用 `POST /meeting-applications` |
| 优先级（高-红、中-黄、低-绿） | ✅ | 首页列表/审批页均按优先级着色（colorError/colorWarning/colorSuccess） |
| 会议审批（授权管理员） | ✅ | `MeetingApprovalPage`：通过/驳回（驳回原因），仅 admin/auth_admin（`meeting-applications.ts:98-99`） |
| 批过的会议才能发起 | ✅ | 预约会议不再直接建会；申请条目在"已批准"后显示"发起会议"按钮 → `POST /meeting-applications/:id/start`（校验 approved，继承预约时间写入 `meetings.scheduled_at`）；首页"我的会议"仅显示已创建（live/scheduled）会议 |
| 优先级低的会议延迟 | ✅ | 需求确认：仅排列展示。`GET /meetings` 按 live > 高 > 中 > 低 > 预约时间排序；申请列表按优先级排序 |
| 快速会议不受影响 | ✅ | "快速会议"磁贴仍直接建会即入会 |

---

### 需求 9：system 用户隐藏 + 系统配置

| 状态 | 分析 |
|------|------|
| ✅ | `sys-user/list` 对非 system 用户屏蔽 `system`（连同 `sysadmin`）；system 为 role_id=1 全权限；`sys.online.maxUsers`（默认 20）**仅 system 账号可见/可管理**（`sys-config.ts` SYSTEM_ONLY_CONFIG_KEYS）；超限提示"当前系统在线人数已达到限制，请联系系统管理员！" |

---

### 需求 8（末尾）：每年免费送 1 次升级

| 状态 | 分析 |
|------|------|
| N/A | 商务条款，非技术实现范畴 |

---

## 本轮修复记录（2026-09-07）

| # | 修复项 | 文件 |
|---|--------|------|
| 1 | 主讲人 PIP 小窗（右下角悬浮、可拖动、主屏显示共享/焦点画面） | `front/src/components/VideoGrid.tsx`、`front/src/index.css`（.video-pip） |
| 2 | 培训右侧列表可收缩/展开，拖动提示走 i18n | `VideoGrid.tsx`、`index.css`（.video-sidelist-toolbar/--collapsed） |
| 3 | 切换焦点画面仅限主持人 | `front/src/pages/MeetingPage.tsx` |
| 4 | 预约会议改走申请审批流（表单含地点/单位数量/优先级；已批准可发起；会议继承预约时间） | `front/src/pages/HomePage.tsx`、`serve/api/src/routes/meeting-applications.ts`、`front/src/api/client.ts` |
| 5 | 管理后台挂载空闲超时退出 | `front/src/components/ManagerShell.tsx` |
| 6 | MemberPicker 编辑时不再清空部门成员 | `front/src/components/MemberPicker.tsx` |
| 7 | 邀请名单显示参会人所属单位；后端返回 deptName | `InvitationPanel.tsx`、`serve/api/src/routes/meetings.ts`、`client.ts` |
| 8 | i18n 补齐（拖动/收缩/邀请名单/申请状态等，zh+en） | `front/src/i18n/locales/{zh,en}.json` |
| 9 | 群组"常用/置顶"标记（迁移 011 + pin 接口 + 星标按钮 + 置顶排序） | `serve/api/src/sql/{sqlite,mysql,postgres}/011_group_pinned.sql`、`serve/api/src/routes/meeting-groups.ts`、`front/src/pages/GroupManagePage.tsx`、`front/src/api/client.ts` |

## 待完善清单（增强项，可选）

| 优先级 | 项目 | 说明 |
|--------|------|------|
| P3 | 密码加密升级 RSA | 现为共享密钥 AES-GCM（密钥随前端分发），非对称方案更符合分保严格口径 |
| P3 | 首页直接查看历史会议邀请名单 | 当前需进入会议（connecting/waiting 阶段）查看 |

## 遗留验证建议

- 双人实测：预约 → 审批 → 发起 → 入会全流程；共享屏幕时主讲人 PIP 位置与拖动。
- 三员账号分别登录验证菜单可见性与操作日志范围。
- 管理后台空闲 10 分钟自动退出生效验证。
