# 需求实现分析清单

> 基于需求文件 `docs/xuqiu/meet-需求-260819.txt`，对照最新代码逐条分析。
> 分析日期：2026-09-03

---

## 需求总览

| 状态 | 数量 | 说明 |
|------|------|------|
| ✅ 已完成 | 14 | 代码已实现，功能可用 |
| ⚠️ 部分完成 | 7 | 后端 API 或核心逻辑已实现，但部分功能缺失（主要是前端页面） |
| ❌ 未实现 | 2 | 代码中未找到对应实现 |

---

## 逐条分析

### 需求 1：会议前小分屏显示被邀请人员入会情况 + 会议开始后主讲人视频可拖动

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 会议前小分屏显示被邀请人员在线/离线 | ⚠️ 部分完成 | **后端已实现**：`meeting_invitations` 表已建（007 迁移）；`GET /meetings/:id/invitations` API 可查询邀请名单+在线状态；`POST /meetings/:id/invite` 可邀请用户/部门。**前端缺失**：`MeetingPage.tsx` 中未找到邀请名单面板的 UI 实现，没有小分屏显示被邀请人员入会情况 |
| 会议开始后主讲人视频默认右下角可拖动 | ❌ 未实现 | `MeetingPage.tsx` 和 `VideoGrid.tsx` 中均未找到拖拽相关代码（`drag`/`draggable`），主讲人视频不支持拖动到任意位置 |

**涉及代码**：
- `serve/api/src/sql/sqlite/007_meeting_invitations.sql` — 数据表 ✅
- `serve/api/src/routes/meeting-groups.ts` — API 路由 ✅
- `serve/realtime/src/signal.ts` — **缺失**：join/leave 时未更新 `meeting_invitations.status` 为 `attended`
- `front/src/pages/MeetingPage.tsx` — **缺失**：邀请名单面板 UI
- `front/src/components/VideoGrid.tsx` — **缺失**：主讲人视频可拖动

---

### 需求 2：会议管理后端查看所有参会者在线状态

| 状态 | 分析 |
|------|------|
| ⚠️ 部分完成 | **后端已实现**：`GET /admin/meetings/online` API 可查询所有进行中会议的参会者在线状态；`GET /meetings/:id/attendees` 可查指定会议参会者。**前端缺失**：系统管理页面中没有 `MeetingMonitorPage.tsx`（设计方案中规划的），`/manager/` 路由下没有会议监控页面 |

**涉及代码**：
- `serve/api/src/routes/meeting-groups.ts` 第 521 行 — `GET /admin/meetings/online` ✅
- `serve/api/src/routes/meeting-groups.ts` 第 478 行 — `GET /meetings/:id/attendees` ✅
- `front/src/pages/manager/` — **缺失**：会议在线状态监控页面

---

### 需求 3：培训模式

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 主讲人有一个小屏显示 | ✅ 已完成 | `VideoGrid.tsx` 中 `training` 布局模式实现了主区域 + 右侧列表的布局 |
| 右侧列表参会人员，点击可切换到所选人员屏幕 | ✅ 已完成 | `onFocusPeer` 回调实现点击切换；培训模式下右侧人员列表可点击切换焦点 |
| 可收缩 | ✅ 已完成 | 布局切换按钮可在 grid/speaker/training 之间切换 |

**涉及代码**：
- `front/src/pages/MeetingPage.tsx` 第 508-519 行 — 布局切换按钮 ✅
- `front/src/components/VideoGrid.tsx` 第 481-521 行 — training 布局 + `onFocusPeer` ✅

---

### 需求 4：一键群组开会

| 状态 | 分析 |
|------|------|
| ✅ 已完成 | `POST /meeting-groups` 创建群组（从系统用户/部门选择成员）；`GET /meeting-groups` 群组列表；`PATCH /meeting-groups/:id` 修改群组；`DELETE /meeting-groups/:id` 删除群组；`POST /meeting-groups/:id/quick-start` 一键群组开会。前端有 `GroupManagePage.tsx` 页面和 `MemberPicker.tsx` 成员选择器组件 |

**涉及代码**：
- `serve/api/src/routes/meeting-groups.ts` — 全部 API ✅
- `front/src/pages/GroupManagePage.tsx` — 群组管理页面 ✅
- `front/src/components/MemberPicker.tsx` — 成员选择器 ✅

---

### 需求 5：发起者可以切换发言者单位画面

| 状态 | 分析 |
|------|------|
| ✅ 已完成 | 培训模式下（`training` 布局），主讲人/主持人可以点击右侧参会者列表中的任意人员，切换主区域显示该人员的视频画面。`onFocusPeer` 回调实现了切换逻辑 |

**涉及代码**：
- `front/src/components/VideoGrid.tsx` 第 498-521 行 — `focusable` + `onFocusPeer` 点击切换 ✅

---

### 需求 6.1：三员账号

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 系统管理员：系统设置管理、添加机构用户 | ✅ 已完成 | `sys_admin` 角色（role_id=3），有用户管理(查询/新增/编辑)+部门管理+参数设置权限 |
| 授权管理员：审核用户增删改操作 | ✅ 已完成 | `auth_admin` 角色（role_id=4），有用户审批+登录日志查询权限 |
| 审计管理员：除自己外所有账号登录操作日志 | ✅ 已完成 | `audit_admin` 角色（role_id=5），有全量操作日志查询/导出/删除权限 |
| 三员分立（符合分保要求） | ✅ 已完成 | 三员权限互斥：系统管理员不能审批、不能看全量日志；授权管理员不能直接操作用户；审计管理员不能操作用户、不能审批 |

**涉及代码**：
- `serve/api/src/sql/sqlite/008_sys_security.sql` — 三员角色种子数据 ✅
- `serve/api/src/seed-admin.ts` — 三员默认账号 ✅
- `serve/api/src/routes/sys-user-approval.ts` — 用户审批 API ✅
- `front/src/pages/manager/UserApprovalPage.tsx` — 用户审批页面 ✅

---

### 需求 6.2：页面停留时间 10 分钟自动退出

| 状态 | 分析 |
|------|------|
| ✅ 已完成 | `useIdleTimer` Hook 实现，默认 600 秒（10 分钟）超时退出，超时前 60 秒弹窗提醒。配置项 `sys.session.idleTimeout`（默认 600）和 `sys.session.idleWarning`（默认 60） |

**涉及代码**：
- `front/src/hooks/useIdleTimer.ts` — 空闲超时 Hook ✅
- `serve/api/src/sql/sqlite/008_sys_security.sql` 第 102-105 行 — 配置项 ✅
- `serve/api/src/index.ts` 第 93-97 行 — `GET /sys-config/public` 公开配置 ✅

---

### 需求 6.3：密码策略

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 首次登录/默认密码登录提示修改密码 | ✅ 已完成 | `isDefaultPassword()` 检测默认密码（`123456,admin123,Admin@123`），返回 `mustChangePassword` 标志 |
| 密码组成复杂（大小写+数字+特殊字符两者以上） | ✅ 已完成 | `password-policy.ts` 实现大小写/数字/特殊字符校验，所有项默认开启 |
| 长度不少于 10 位 | ⚠️ 部分完成 | 默认 `minLength=8`，**与需求要求的 10 位不符**。但该项可配置，修改为 10 即可满足 |
| 定期更换密码（15 天） | ⚠️ 部分完成 | `isPasswordExpired()` 已实现过期检测。默认 `expireDays=90`，**与需求要求的 15 天不符**。但该项可配置，修改为 15 即可满足 |

**涉及代码**：
- `serve/api/src/password-policy.ts` — 密码策略模块 ✅
- `serve/api/src/routes/auth.ts` — 登录时检测 ✅
- `serve/api/src/sql/sqlite/008_sys_security.sql` 第 72-93 行 — 配置项 ⚠️（默认值不符）

> **建议**：将 `sys.password.minLength` 默认值改为 10，`sys.password.expireDays` 默认值改为 15，以匹配需求要求。

---

### 需求 6.4：密码加密传输，不得明文传输

| 状态 | 分析 |
|------|------|
| ⚠️ 部分完成 | 密码通过 HTTPS 加密传输（部署文档强调生产强制 HTTPS）。但前端**未做应用层加密**（如 RSA 加密），密码以明文形式通过 HTTP body 发送，依赖 TLS 层保护。设计方案中优先级 P3，标注「依赖 HTTPS 也可满足基本要求」 |

**涉及代码**：
- `front/src/pages/LoginPage.tsx` 第 35 行 — `password: values.password` 明文 ⚠️
- HTTPS 部署 — 文档已说明 ✅

> **说明**：若需满足分保要求严格意义上的"加密传输"，建议在应用层增加 RSA/AES 加密。当前方案依赖 HTTPS 传输层加密。

---

### 需求 6.5：用户锁定/解锁

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 连续失败 5 次锁定 | ✅ 已完成 | `checkAccountLock()` + `recordLoginFailure()` 实现，默认 5 次失败锁定 |
| 形成告警事件 | ✅ 已完成 | `recordLoginLog()` 记录登录失败日志（含 `account_locked` 原因） |
| 1 分钟后自动解除 | ✅ 已完成 | 默认 `lockDuration=60` 秒，Redis TTL 自动过期解锁 |

**涉及代码**：
- `serve/api/src/routes/auth.ts` 第 26-72 行 — 锁定/解锁逻辑 ✅
- `serve/api/src/sql/sqlite/008_sys_security.sql` 第 108-111 行 — 配置项 ✅

---

### 需求 6.6：日志

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 按字段条件查询 | ✅ 已完成 | `GET /sys-operlog/list` 支持按时间范围、操作人、模块、状态筛选 |
| 支持日志导出 | ✅ 已完成 | `GET /sys-operlog/export` 导出日志 |
| 日志包含必要字段 | ✅ 已完成 | `sys_oper_log` 表含：操作时间、IP、操作人、模块、操作类型、请求参数、响应结果、状态 |
| 至少保留 12 个月 | ✅ 已完成 | 默认 `retentionDays=365`（=12 个月），定时自动清理 |
| 存储空间阈值设置 | ✅ 已完成 | `sys.log.storageThreshold` 配置项（默认 80%） |

**涉及代码**：
- `serve/api/src/routes/sys-operlog.ts` — 日志查询/导出/删除 ✅
- `serve/api/src/middleware/oper-log.ts` — 自动记录中间件 ✅
- `serve/api/src/sql/sqlite/008_sys_security.sql` 第 114-117 行 — 配置项 ✅
- `front/src/pages/manager/OperLogPage.tsx` — 日志查询页面 ✅

---

### 需求 7：省里会议投屏到市会议界面

| 状态 | 分析 |
|------|------|
| ❌ 未实现（需求已标注暂不实现） | 需求文件已标注「暂不实现，需要硬件支持」 |

---

### 需求 8：会议申请审批

| 子需求 | 状态 | 分析 |
|--------|------|------|
| 会议申请（时间、地点、单位数量、会议名称、优先级） | ✅ 已完成 | `meeting_applications` 表含全部字段；`POST /meeting-applications` 提交申请 |
| 优先级（高-红、中-黄、低-绿） | ✅ 已完成 | 前端 `MeetingApprovalPage.tsx` 中 `priorityColors` 映射：高=red、中=orange、低=green |
| 会议审批（授权管理员审批） | ✅ 已完成 | `PATCH /meeting-applications/:id/approve` 仅 `admin`/`auth_admin` 可操作 |
| 批过的会议才能发起 | ✅ 已完成 | `POST /meeting-applications/:id/start` 校验 `status=approved` |
| 优先级低的会议延迟 | ⚠️ 部分完成 | API 查询按优先级排序（高>中>低）。但需求确认中明确为"预约会议后被邀请人员看到的右侧会议列表显示排序的优先级"，前端首页 `HomePage.tsx` 未按优先级排序显示 |

**涉及代码**：
- `serve/api/src/routes/meeting-applications.ts` — 全部 API ✅
- `serve/api/src/sql/sqlite/009_meeting_applications.sql` — 数据表 ✅
- `front/src/pages/manager/MeetingApprovalPage.tsx` — 审批页面 ✅
- `front/src/pages/HomePage.tsx` — **缺失**：优先级排序显示

---

### 需求 9：特定用户 system 隐藏藏 + 系统配置

| 子需求 | 状态 | 分析 |
|--------|------|------|
| system 用户默认在用户管理不可见 | ✅ 已完成 | `sys-user.ts` 第 132-134 行：非 system 用户查询时 `WHERE u.user_name != 'system'` |
| system 用户拥有所有权限 | ✅ 已完成 | seed 中 `system` 用户分配 role_id=1（超级管理员），全部菜单权限 |
| 系统配置：同时在线人数默认 20 人 | ✅ 已完成 | `sys.online.maxUsers` 配置项默认 20 |
| 超出限制提示"请联系系统管理员" | ✅ 已完成 | `signal.ts` 第 290-292 行：`online_limit_reached` 错误消息 |

**涉及代码**：
- `serve/api/src/routes/sys-user.ts` 第 132-134 行 — system 用户隐藏 ✅
- `serve/api/src/seed-admin.ts` — system 用户创建 ✅
- `serve/api/src/sql/sqlite/008_sys_security.sql` 第 120 行 — 在线人数配置 ✅
- `serve/realtime/src/signal.ts` 第 272-292 行 — 在线限制检查 ✅

---

### 需求 8（末尾）：每年免费送 1 次升级

| 状态 | 分析 |
|------|------|
| N/A | 此为商务条款，非技术实现范畴 |

---

## 汇总表

| 需求编号 | 需求内容 | 状态 |
|---------|---------|------|
| 1 | 会议前小分屏显示被邀请人员入会情况 | ⚠️ API 已实现，前端 UI 缺失 |
| 1 | 会议开始后主讲人视频可拖动 | ❌ 未实现 |
| 2 | 会议管理后端查看参会者在线状态 | ⚠️ API 已实现，前端页面缺失 |
| 3 | 培训模式 | ✅ 已完成 |
| 4 | 一键群组开会 | ✅ 已完成 |
| 5 | 发起者切换发言者画面 | ✅ 已完成 |
| 6.1 | 三员账号 | ✅ 已完成 |
| 6.2 | 页面 10 分钟自动退出 | ✅ 已完成 |
| 6.3 | 密码策略 | ⚠️ 功能已实现，默认值不符（minLength=8 应为 10，expireDays=90 应为 15） |
| 6.4 | 密码加密传输 | ⚠️ 依赖 HTTPS，未做应用层加密 |
| 6.5 | 用户锁定/解锁 | ✅ 已完成 |
| 6.6 | 日志查询/导出/保留 | ✅ 已完成 |
| 7 | 省里投屏到市会议 | ❌ 暂不实现（需硬件支持） |
| 8 | 会议申请审批 | ✅ 已完成 |
| 8 | 优先级低的会议延迟排序 | ⚠️ API 排序已实现，前端首页未按优先级显示 |
| 9 | system 用户隐藏 | ✅ 已完成 |
| 9.1 | 在线人数限制 | ✅ 已完成 |

---

## 待完善清单

以下为需要补全的功能项，按优先级排序：

| 优先级 | 待完善项 | 涉及模块 |
|--------|---------|---------|
| P1 | 信令服务 join/leave 时更新 `meeting_invitations.status` | `serve/realtime/src/signal.ts` |
| P1 | 会议页面邀请名单面板（小分屏显示在线/离线） | `front/src/pages/MeetingPage.tsx` |
| P1 | 会议在线状态监控页面（系统管理后端） | `front/src/pages/manager/MeetingMonitorPage.tsx`（新建） |
| P2 | 主讲人视频可拖动（默认右下角） | `front/src/components/VideoGrid.tsx` |
| P2 | 首页会议列表按优先级排序显示 | `front/src/pages/HomePage.tsx` |
| P3 | 密码策略默认值调整（minLength→10，expireDays→15） | `serve/api/src/sql/*/008_sys_security.sql` |
| P3 | 密码应用层加密传输（RSA/AES） | `front/src/pages/LoginPage.tsx` + `serve/api/src/routes/auth.ts` |
